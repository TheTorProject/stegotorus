Immediate priority:

  - Finish designing and coding a serious 1:N protocol with crypto and
    optional steg
    - Get more eyes on chopper crypto

  - SOCKS support for chop.c

  - HTTP steg should honor query-response pattern

  - More steg modules

Documentation:

  - Document the current threat model
  - Document the chopper protocol, the steg modules, and the
    current APIs in detail
  - Write end-user documentation

Cleanup:

  - C++ conversion
  - real booleans
  - do something about the size_t-to-unsigned long casts all over the place
  - stegotorus / test_stegotorus binary split
  - make socks a protocol or something,
    rather than a special case in the generic code

Testing:

  - more tl_* test patterns
  - unit tests for the chopper and the steg layer
  - better timeout handling in itestlib?
  - make the Python tests work on Windows

Misc:

  - configuration file support
  - genericize configuration?
  - Make it obey the xxx-pluggable-transport spec in Tor.
